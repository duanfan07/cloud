server:
  port: 80

eureka:
  client:
    register-with-eureka: false   #向注册中心注册自己
    service-url:
      defaultZone: http://localhost:7001/eureka/,http://localhost:7002/eureka/   #注册中心地址

ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000
logging:
  level:
    com.example.PaymentFeignService: debug
